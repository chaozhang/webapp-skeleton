
const initialState = {
    type: 'home',
    data: `Chao's web app depends on ChaoWebCommon, webapp-skeleton a quick setup with React + Redux`
};

const homeReducer = (state = initialState, action) => {
    return state;
}


export default homeReducer