
const initialState = {
    type: 'technologies',
    data: [
        'React', 'Redux', 'Stylus',
        'webpack', 'Redux-saga', 'gh-pages',
        'react-router-dom'
    ]
};

const reducer = (state = initialState, action) => {
    return state;
}


export default reducer