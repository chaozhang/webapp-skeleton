
const initialState = {
    type: 'dependency',
    data: 'Leverage ChaoWebCommon for all reusable common components (header, nav, footer, icons...)'
};

const reducer = (state = initialState, action) => {
    return state;
}


export default reducer